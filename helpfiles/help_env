env: env
	Display environment variable

	Display all available environment variables to screen. If any arguments
	are specified, then shell will throw an error for user.

	Options:
		There are no available options.

	Exit status:
		Return 0 for success, 1 for failure or error.
