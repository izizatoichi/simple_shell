setenv: setenv [VARIABLE] [VALUE]
	Create new environment variable or modify existing one.

	Note: Arguments VARIABLE and VALUE are space deliminated.

	Initialize a new environment VARIABLE with VALUE if VARIABLE does not
	exist in list of environment variables. If VARIABLE exists in list,
	then its VALUE will be modified to the new VALUE passed in.

	setenv takes exactly two arguments. Passing in fewer than two will
	throw an error. Passing in more than two will result in the later
	arguments being ignored.

	Options:
		There are no options available.

	Exit Status:
		Return exit code 0 for success and 1 for error.
